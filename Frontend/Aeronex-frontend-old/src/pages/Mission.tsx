import { AlertPanel } from '../components/dashboard/AlertPanel'; import { MissionMeter } from '../components/dashboard/MissionMeter'; import { MissionTimeline } from '../components/dashboard/MissionTimeline'; import type { ReturnTypeSimulation } from './pageTypes'
export function Mission({sim}:{sim:ReturnTypeSimulation}){const m=sim.snapshot.mission;return <><div className="page-heading"><div><p>MISSION</p><h1>{m.name}</h1></div><span>ENGINE HEALTH <b>{sim.snapshot.health}</b></span></div><div className="dashboard-grid"><MissionMeter progress={m.progress} phase={m.currentPhase} reliability={m.reliability}/><section className="panel mission-overview"><p>MISSION OVERVIEW</p><h3>{m.currentPhase}</h3><span>Backend state is authoritative when connected. The local provider is only used in DEMO MODE.</span></section></div><MissionTimeline phases={m.phases}/><AlertPanel alerts={sim.snapshot.alerts}/></>}
