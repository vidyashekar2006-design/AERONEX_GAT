import { AlertPanel } from '../components/dashboard/AlertPanel'; import { DegradationPanel } from '../components/dashboard/DegradationPanel'; import { HealthPanel } from '../components/dashboard/HealthPanel'; import type { ReturnTypeSimulation } from './pageTypes'
export function HealthAlerts({sim}:{sim:ReturnTypeSimulation}){const {controls,snapshot}=sim;return <><div className="page-heading"><div><p>HEALTH & ALERTS</p><h1>Condition Monitoring</h1></div><span>STATUS <b>{snapshot.health}</b></span></div><div className="dashboard-grid three"><HealthPanel health={snapshot.health}/><DegradationPanel scenario={controls.scenario} enabled={controls.enabled} severity={controls.severity} onScenario={controls.setScenario} onEnabled={controls.setEnabled} onSeverity={controls.setSeverity}/><section className="panel unavailable-panel"><p>ANOMALIES & FAULTS</p><h3>NOT AVAILABLE</h3><span>Awaiting validated model output</span></section></div><AlertPanel alerts={snapshot.alerts}/></>}
