import { TelemetryGrid } from '../components/dashboard/TelemetryGrid'; import { TelemetryCharts } from '../components/dashboard/TelemetryCharts'; import type { ReturnTypeSimulation } from './pageTypes'
export function LiveTelemetry({sim}:{sim:ReturnTypeSimulation}){return <><div className="page-heading"><div><p>LIVE TELEMETRY</p><h1>Telemetry Monitor</h1></div><span className="demo">DEMO MODE — LOCAL PROVIDER</span></div><div className="operating-strip"><span>OPERATING MODE <b>{sim.snapshot.operatingMode}</b></span><span>SAMPLING <b>1.8 s</b></span><span>ALTITUDE <b>{sim.snapshot.environment.altitude} m</b></span><span>AMBIENT <b>{sim.snapshot.environment.ambientTemperature} °C</b></span></div><TelemetryGrid telemetry={sim.snapshot.telemetry}/><TelemetryCharts telemetry={sim.snapshot.telemetry} large/></>}
