import { AnalysisPanel } from '../components/dashboard/AnalysisPanel'; import { MissionMeter } from '../components/dashboard/MissionMeter'; import type { ReturnTypeSimulation } from './pageTypes'
export function Analysis({sim}:{sim:ReturnTypeSimulation}){return <><div className="page-heading"><div><p>ANALYSIS</p><h1>AI & Model Outputs</h1></div><span className="source-tag">NO VALIDATED MODELS CONNECTED</span></div><AnalysisPanel/><MissionMeter progress={sim.snapshot.mission.progress} phase={sim.snapshot.mission.currentPhase} reliability={sim.snapshot.mission.reliability}/></>}
