const cards=[['EVENT','Operational events and alerts from the future backend stream.'],['REPORT','Prepared interface for maintenance and mission report exports.'],['CONFIGURATION','Versioned configuration and snapshot records.']]
export function Reports(){return <><div className="page-heading"><div><p>REPORTS</p><h1>ERC Interface</h1></div><span className="demo">UI PREVIEW</span></div><section className="erc-grid">{cards.map(([label,description])=><article className="panel" key={label}><p>{label}</p><h2>{label}</h2><span>{description}</span><button disabled>AWAITING BACKEND</button></article>)}</section></>}
