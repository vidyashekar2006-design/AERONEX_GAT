import type { Reliability } from '../../simulation/types'
export function MissionMeter({progress,phase,reliability}:{progress:number;phase:string;reliability:Reliability}) { return <section className="panel mission-meter"><div className="section-head"><div><p>MISSION METER</p><h3>MISSION PROGRESS</h3></div><strong>{progress}%</strong></div><div className="progress"><i style={{width:`${progress}%`}}/></div><div className="current-phase">CURRENT PHASE <b>{phase}</b></div><div className="reliability"><p>MISSION RELIABILITY</p><strong>{reliability.status.replaceAll('_',' ')}</strong><span>Prediction status: MODEL PENDING</span><small>{reliability.reason}</small></div></section> }
