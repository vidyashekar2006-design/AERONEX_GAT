# PostgreSQL is the production/default database. Do not place real credentials here.
DATABASE_URL=postgresql+asyncpg://aeronex:aeronex@localhost:5432/aeronex
# Used only when USE_SQLITE=true (development and isolated tests).
SQLITE_DATABASE_URL=sqlite+aiosqlite:///./aeronex.db
USE_SQLITE=false
TEST_DATABASE_URL=
